name: 'Buildpack CI'
description: 'Easily run builds and unit tests with minimal configuration efforts by using buildpacks a la Heroku'
runs:
  using: 'docker'
  image: 'Dockerfile'
branding:
  icon: 'check-circle'
  color: 'red'
